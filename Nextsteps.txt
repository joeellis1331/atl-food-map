SUGGESTED/POTENTIAL ENHANCEMENTS
Main.py
- Caputure the final output, checks against new xlsx and only runs new geocode or makes
    changes when there isn't a match
- capture geocoding errors into a log file

map_choropleth.py
- Adjust colors to highlight differences more. Currently they all look very similar on initial eye inspection.
    Also make sure they are okay for people with color blindness

Index.html
- Convert main page headings to Maps, Lists, Superlatives. Where Maps contains a drop down
    of all and best neighborhoods. Lists contains a drop down of each of the 5

spreadsheets tabs
- Use datatables (https://datatables.net/) to improve the spreadsheet functionality with
    my static webpage. It should work without having to host something on the cloud or whatever
- change into nav bar dropdown with each spreadsheet then having a title rather than the second
    series of headings


utils_choropleth.py
- maybe add a rank column to the dataframe and present a list of the top ranked neighborhoods. Set up
    similar to spreadsheets where you can toggle either map element or table element?

utils_map_elements.py
- add a search function to zoom in on an address
    - mile search radius within that address?
    - maybe even a route finding function?


########### Notes about branches
bottom left of VS code window, click on branch name (main or development) to pull up menu as to which branch
to switch to. Created development branch to test new changes without breaking the HTML. It still feels complex
to me so I need that fallback

#food color palettes
https://colorhunt.co/palettes/food


###############
indv_watermark = """
    {% macro html(this, kwargs) %}
    <style>
        #draft-watermark {
            position: fixed;
            top: 10px;
            left: 50%;
            transform: translateX(-50%);
            background-color: rgba(255, 0, 0, 0.6);
            color: white;
            padding: 5px 12px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 4px;
            z-index: 9999;
            pointer-events: none;
        }
    </style>
    <div id='draft-watermark'>DRAFT VERSION</div>
    {% endmacro %}
    """